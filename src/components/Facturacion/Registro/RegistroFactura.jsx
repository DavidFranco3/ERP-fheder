import { useState, useEffect } from 'react';
import {Alert, Col, Row} from "react-bootstrap";

function RegistroFactura(props) {
    const { setShowModal, location, history } = props;

    return (
        <>
        </>
    );
}

export default RegistroFactura;
